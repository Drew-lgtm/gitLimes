[package]
name = "gitlimes"
version = "0.1.0"
edition = "2021"
description = "Fast, low-memory git history CLI"

[dependencies]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
